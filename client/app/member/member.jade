header(ng-controller='NavbarCtrl')
  .container
    div.navbar-header
      button.navbar-toggle(type='button', ng-click='isCollapsed = !isCollapsed')
        span.sr-only Toggle navigation
        span.icon-bar
        span.icon-bar
        span.icon-bar
      a.navbar-brand(href='/') Design Chips
      
    nav.navigation__global--default(collapse='isCollapsed')
      ul.navmenu__toggle--default
        li.navmenu__child--default(ng-repeat='item in menu', ui-sref-active='active')
          a child
        li.navmenu__child--default(ng-repeat='item in menu', ui-sref-active='active')
          a child
        li.navmenu__child--default(ng-repeat='item in menu', ui-sref-active='active') 
          a child
        
      ul.navmenu__toggle--right
        li.navmenu__child--default
          a GitHub
      
main
  .container
    h1 main section
  
footer
